---
title: Zone Builder — Component Resize — Size Control in Properties Panel
type: Feature
priority: M
status: pending
area: slides
---

Add a size control to the Zone Builder properties panel so users can resize components. Options should be compact / default / wide, mapping to flex or width values on the `.zb-component` wrapper so the component takes up less or more horizontal space within its slot.
