---
title: Builder — Preview — Header — Move nav bar inside slide container
priority: normal
status: pending
area: builder
---

The `#header` nav bar in `builder/features/builder-ui/preview.html` is currently rendered as a fixed top bar outside the slide area. Move it so it renders inside the `#header` element that wraps the slide container, matching the same position/structure as the presentation viewer's header.
