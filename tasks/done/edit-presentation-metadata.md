---
title: Edit presentation metadata
priority: normal
status: pending
area: dashboard-ui
---

Allow editing the presentation Name, Contact name, and Contact position directly from the Dashboard (inline or via a modal). Changes should be saved back to `presentations.json` via `PUT /api/presentations/:id`.
