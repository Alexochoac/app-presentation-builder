---
title: Builder — Presentation — Translation — Translate all editable fields at once via API
priority: normal
status: pending
area: builder
---

Add a "Translate" button to the builder that collects all data-edit field values from the current presentation, sends them to a translation API (Claude API recommended), and replaces each field with the translated text in the selected target language. The user should be able to pick the target language before triggering the translation.
